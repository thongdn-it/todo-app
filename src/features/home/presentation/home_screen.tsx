import React from 'react';
import {Button, Text, View} from 'native-base';

import {TDHomeScreenProps} from '../../../routes/props';

export const TDHomeScreen = (_: TDHomeScreenProps) => {
  return (
    <View>
      <Button>
        <Text>AAA</Text>
      </Button>
    </View>
  );
};
